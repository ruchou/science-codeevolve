"""SingleModelEnsemble — minimal BaseEnsemble wrapping a single LM. Used
for the --ea-strategy claude_code path so the rest of evolution.py can
treat the CC-only configuration as just another ensemble."""
